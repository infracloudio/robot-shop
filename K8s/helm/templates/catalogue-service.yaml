apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    app.kubernetes.io/name: robot-shop
    service: catalogue
spec:
  ports:
  - name: http
    port: 8080
  selector:
    service: catalogue
